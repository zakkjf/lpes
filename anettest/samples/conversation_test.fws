/*
   Simple conversation test. The work of smtp client will be imitated using info from trace file.

   rus as:
      anettest -d eth0#0 -d eth1#1 -f this_file.fws

*/

TRACE "smtp_client" // specifies the template to imitate

INCLUDE configSession  // requied configuration of test

run accept 1-     // runs test, request to the result of test is specified: all packets must be successfully passed through